<p>
	<strong>Monano</strong>はInternet上に存在する新しい<strong>Monano</strong>です (意味不明)。<br>
	<strong><%= settings.price %>Mona</strong>/口で発売中
	
	<form action='/buy' method="POST">
    <input type="text" placeholder="address" name="address">
    <input type="number" placeholder="amount" name="amount">
    <input type="submit" value="Buy">
	</form>
	
	<table>
    <tr>
      <th>ID</th>
			<th>数量</th>
			<th>購入者アドレス</th>
			<th>支払額</th>
			<th>当選額</th>
		</tr>
    <% @packs.each do |pack| %>
		  <tr>
				<td class="right"><%= pack.id %></td>
				<td class="right"><%= pack.amount %></td>
				<td><%= pack.payout_address %></td>
				<td class="right"><%= pack.paid %></td>
				<td class="right"><%= pack.payouted %></td>
			</tr>
		<% end %>
	</table>
</p>